# Legacy Changelog (Archived)

This repository previously maintained a root `CHANGELOG.md`.

That file was removed during next-phase cleanup because it no longer reflected
the current runtime-first Nexus surface and release process.

Release artifacts and versioned binary outputs are now tracked via CI workflows:

- `.github/workflows/build-release.yml`
- GitHub Releases artifacts for tag builds
- Main-branch build artifacts uploaded from CI

For historical change context before this archive, use Git history.
