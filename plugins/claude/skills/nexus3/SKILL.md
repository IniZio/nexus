---
name: nexus3
description: nexus3 microVM sandbox — lifecycle, volumes, mounts, and egress configuration.
---

# nexus3

STUB — slice-A owns this body. Replace with the split dispatcher and references/ drawn from skills/nexus3/SKILL.md at the repo root.

Load this skill when the user asks about nexus3 sandbox operations: create, start, stop, shell, exec, snapshot, fork, volume, mount, or egress.

See `nexus3 --help` for the full command surface, or invoke `nexus3 mcp` for MCP tool access.
