# nexus3 Claude Code plugin

Registers the `nexus3` skill namespace and the `nexus3 mcp` stdio server for use in any Claude Code session.

## Install

```
make install-plugin
```

Creates `~/.claude/plugins/nexus3 -> $HOME/nexus3/plugins/claude`.

## Uninstall

```
rm ~/.claude/plugins/nexus3
```
