// Package infra provides shared infrastructure components used across the daemon.
// It groups persistence (store), credential relay, and other cross-cutting concerns
// that do not belong to a single domain.
package infra
