{
  "name": "@nexus/e2e-tests",
  "version": "0.1.0",
  "description": "End-to-end tests for Nexus (currently disabled - pending rewrite)",
  "private": true,
  "scripts": {
    "test:e2e": "echo 'E2E tests are disabled - pending rewrite to use nexus CLI instead of deleted workspace-sdk' && exit 0",
    "test:integration": "echo 'Integration tests are disabled - pending rewrite' && exit 0",
    "test": "echo 'Tests are disabled - see README.md' && exit 0",
    "test:watch": "echo 'Tests are disabled' && exit 0",
    "test:coverage": "echo 'Tests are disabled' && exit 0",
    "docker:up": "echo 'Docker environment not available - workspace-sdk was deleted' && exit 1",
    "docker:down": "echo 'Docker environment not available' && exit 1",
    "docker:build": "echo 'Docker environment not available' && exit 1"
  },
  "keywords": [
    "nexus",
    "e2e",
    "tests"
  ],
  "author": "Nexus Team",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
